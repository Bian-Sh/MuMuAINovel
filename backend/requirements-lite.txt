# Web框架
fastapi==0.121.0
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# 数据库
sqlalchemy==2.0.25
asyncpg==0.29.0    # PostgreSQL异步驱动（仅迁移脚本需要，可选）
psycopg2-binary==2.9.9  # PostgreSQL同步驱动（仅迁移脚本需要，可选）
alembic==1.14.0  # 数据库迁移工具
aiosqlite==0.22.1

# 数据验证
pydantic==2.12.4
pydantic-settings==2.11.0

# AI服务（OpenAI 兼容 API，例如 Hermes、MiniMax 等）
openai==2.7.0
anthropic==0.72.0

# 工具库
httpx==0.28.1
python-dotenv==1.1.0
psutil==6.1.1
aiosmtplib==4.0.2
mcp==1.22.0         # Model Context Protocol Python SDK

# ============================================================
# 以下依赖已移除（精简版不需要本地 AI 模型）
# 移除: chromadb==1.3.2
# 移除: transformers==4.57.1
# 移除: sentence-transformers==5.1.2
# 移除: torch==2.8.0
# ============================================================
